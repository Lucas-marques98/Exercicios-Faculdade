const [cabecalho] = require("../Biblioteca/cabecalho");
var prompt = require("prompt-sync")();
cabecalho("Programa para mostrar a soma");


for (let numero = 0; numero <= 10; numero++) {
    console.log(`7 x ${numero} = ${7 * numero}`);

}